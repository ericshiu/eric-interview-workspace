apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform.fullname" . }}-svc
  namespace: asiayo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: asiayo-web